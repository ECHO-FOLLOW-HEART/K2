# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
#
# This must be changed for production, but we recommend not changing it in this file.
#
# See http://www.playframework.com/documentation/latest/ApplicationSecret for more details.
application.secret = "D/:X>L;Ud7ZLSWCd8r=9Yjs>YjEHefUrW;yZc?V>rJjx;BAc@4OG5Eoym9VAlpxi"

# The application languages
# ~~~~~
application.langs = "en"

# Global object class
# ~~~~~
# Define the Global object class for this application.
# Default to Global in the root package.
application.global = Global

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your route file is named properly.
# So for an application router like `conf/my.application.Router`,
# you may need to define a router file `my.application.routes`.
# Default to Routes in the root package (and `conf/routes`)
# application.router=my.application.Routes

# Database configuration
# ~~~~~
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`
#
# db.default.driver = org.h2.Driver
# db.default.url = "jdbc:h2:mem:play"
# db.default.user = sa
# db.default.password = ""

#db.default.driver=com.mysql.jdbc.Driver
#db.default.url="jdbc:mysql://localhost:3306/vxp"
#db.default.user=vxp
#db.default.password="vxp123"

#
# You can expose this datasource via JNDI if needed (Useful for JPA)
# db.default.jndiName=DefaultDS

# Evolutions
# ~~~~~
# You can disable evolutions if needed
evolutionplugin = disabled

# Ebean configuration
# ~~~~~
# You can declare as many Ebean servers as you want.
# By convention, the default server is named `default`
#
# ebean.default = "models.*"

# db.default.logStatements=true

# logger.com.jolbox=DEBUG

# Logger
# ~~~~~
# You can also configure logback (http://logback.qos.ch/),
# by providing an application-application-logger.xml file in the conf directory.

# Root logger:
# logger.root=ERROR

# Logger used by the framework:
#logger.play=INFO

# Logger provided to your application:
#logger.application=DEBUG

# MongoDB:
mongodb.serverList = []

# 短信网关设置
sms.scheme = http
sms.resendInterval = 90
sms.signupExpire = 1200

# Solr配置
solr.host = localhost
solr.port = 8983
solr.core = travelnote
solr.viewspot = viewspot

# 环信
easemob.org = aizou
easemob.app = taozi

# 七牛
qiniu.avaterScope = lvxingpai-img-store
qiniu.taoziAvaterScope = taozi-uploads
qiniu.callbackUrl = api.taozilvxing.cn/taozi/misc/upload-callback

# memcache
ehcacheplugin = disabled
memcached.enabled = false
memcachedplugin = disabled

# 旅行派
share.domain = wap.lvxingpai.com

share.url = sharePlan.php

# 交通预算与行程距离的比率，交通预算=行程距离*比率
budget.trafficBudgetRatio = 0.8d

# 默认的交通预算，在交通预算计算失败时，取此值
budget.trafficBudgetDefault = 800d

# 默认的景点预算，景点价格为空时，取此值
budget.viewpointBudgetDefault = 30d

# 默认的每天食宿费
budget.stayBudgetDefault = 300

# 图片尺寸
image.big = imageView2/2/w/800/h/600/q/85/format/jpg/interlace/1
image.small = imageView2/2/w/400/h/300/q/85/format/jpg/interlace/1

#-------------------------------------------------------------------
# 桃子旅行-微信请求用户信息链接
wx.appid = wx26b58c7173483529
wx.secret = 28daa05c021ebebe6d3cf06645b0c5ac
wx.domain = api.weixin.qq.com
wx.urlaccess = /sns/oauth2/access_token
wx.urlinfo = /sns/userinfo

# 桃子旅行-用户用短信验证码换取token的token失效时间,单位:秒
token.expireTime = 300

# 桃子旅行-运管内容HTML5页面的请求链接
h5.host = h5.taozilvxing.com
# 运营专栏
h5.column = /homepage.php
# 推荐页的运营内容
h5.recom = /homepage.php

#memcached logger
#logger.memcached=DEBUG

#mom
mom.host = localhost
mom.port = 5672
mom.durable = true
mom.exchangeType = topic

include "destinations.conf"
include "loc_mapping.conf"

include "local.conf"
