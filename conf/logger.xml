<?xml version="1.0" encoding="UTF-8" ?>
<!--默认情况下是每分钟重新读取的配置文件 scanPeriod="30 seconds"-->
<configuration scan="true" scanPeriod="5 minutes">
    <!--<timestamp key="startTime" datePattern="yyyyMMdd'T'HHmmss"/>-->
    <!--设置创建时间-->
    <timestamp key="date" datePattern="yyyyMMdd"/>
    <timestamp key="startTime" datePattern="yyyyMMdd'T'HHmmss"/>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${application.home}/logs/coreapi-${date}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>${application.home}/logs/coreapi-${startTime}.%i.log.zip</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>100</maxIndex>
        </rollingPolicy>
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <maxFileSize>10MB</maxFileSize>
        </triggeringPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg [%file:%line] %n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%coloredLevel %logger{15} - %message%n%xException{5}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <logger name="com.travelpi" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="FILE"/>
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>